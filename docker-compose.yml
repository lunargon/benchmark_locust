version: "3.8"
services:
  benchmark:
    container_name: benchmark
    image: benchmark
    build:
      context: .
      dockerfile: Dockerfile
    hostname: benchmark
    restart: always
    ports:
      - "8089:8089"
    command: >
      bash -c "locust"